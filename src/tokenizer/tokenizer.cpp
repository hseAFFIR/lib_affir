#include "tokenizer.h"
#include <algorithm>

// Реализация конструктора
Tokenizer::Tokenizer(const std::vector<Base*>& filters) 
    : filters(filters) 
{
    // Дополнительная инициализация при необходимости
}
